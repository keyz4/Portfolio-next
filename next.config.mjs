/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['pbs.twimg.com','images.unsplash.com','upload.wikimedia.org','miro.medium.com'],
      }
};

export default nextConfig;
